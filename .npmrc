@poukai-inc:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
# Force auth on EVERY request to this host, not just registry metadata.
# GitHub Packages requires a token on the tarball download too; with the
# default always-auth=false, pnpm fetches metadata authed but the
# npm.pkg.github.com/download/... tarball UNauthed → 401. This is the real
# cause of the @poukai-inc/ui install 401 (masked for ages by the pnpm cache).
//npm.pkg.github.com/:always-auth=true

# pnpm 10 default flipped link-workspace-packages to false. We rely on
# the dual-consumption model: locally a parent pnpm-workspace.yaml lists
# both repos, pnpm should prefer the workspace @poukai-inc/ui. In CI
# there's no workspace, so this setting has no effect and pnpm fetches
# from the registry above.
link-workspace-packages=true
